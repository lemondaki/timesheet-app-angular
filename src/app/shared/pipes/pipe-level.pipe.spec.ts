import { PipeLevelPipe } from './pipe-level.pipe';

describe('PipeLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
